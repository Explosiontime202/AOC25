# Advent of Code 2025

My solve scripts from AOC 2025. The solve scripts for every day can be execute with the following command:

```bash
cargo run --release <number-of-the-day> <input-file>
```

For example:

```bash
cargo run --release 3 example.txt
```
